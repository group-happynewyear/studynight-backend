spring:
  application:
    name: "studynight"

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    open-in-view: false

  profiles:
    active: test

token:
  refresh:
    expiration-days: 14

logging:
  level:
    kr.happynewyear: debug
